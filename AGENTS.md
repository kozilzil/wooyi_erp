# AGENTS.md

## 1. 프로젝트 목표

이 프로젝트는 교회 내부망에서 운영되는 ERP를 구축하는 것이다.
핵심 모듈은 아래 3개다.

1. 교적관리
2. 재정관리
3. 비품관리

이 저장소에서 작업하는 에이전트는 **기능 구현**뿐 아니라 **문서 일관성**, **테스트 통과**, **감사 가능성**까지 고려해야 한다.

## 2. 절대 바꾸면 안 되는 결정사항

다음 사항은 고정값이다. 추정하거나 임의 변경하지 말 것.

- 모바일은 전용 앱이 아니라 **반응형 웹만 제공**
- 데이터베이스는 **MariaDB**
- 재정 모듈은 **단식부기와 복식부기를 모두 지원**
- QR은 시스템 내부 직접 인쇄가 아니라 **외부 라벨 인쇄 프로그램 연동**
- 백엔드는 **Spring Boot**
- 프런트엔드는 **React**
- 모든 중요 변경은 **감사로그**를 남겨야 함
- 재정 승인/취소/마감은 반드시 **트랜잭션 단위**로 처리해야 함
- 삭제는 특별한 사유가 없으면 **논리 삭제 우선**
- 권한은 **메뉴 권한**과 **데이터 권한**을 분리하여 고려

## 3. 작업 시작 전 필수 확인 문서

작업 전 아래 순서로 문서를 읽는다.

1. `README.md`
2. `docs/01-product-scope.md`
3. `docs/03-architecture.md`
4. `docs/04-erd.md`
5. `docs/05-api-contracts.md`
6. 현재 작업 카드 (`docs/07-backlog/*.md`)

작업 카드와 상위 문서가 충돌하면:
- 우선 `AGENTS.md`
- 다음 `docs/03-architecture.md`
- 다음 해당 backlog 카드
- 마지막으로 기타 문서

## 4. 구현 규칙

### 4.1 공통
- 한 번에 너무 큰 범위를 수정하지 말 것
- 기능은 backlog 카드 단위로 구현할 것
- 새로운 API를 추가하면 API 문서를 함께 갱신할 것
- 새로운 엔터티를 추가하면 ERD 문서와 마이그레이션을 함께 갱신할 것
- 권한 영향을 주는 기능은 권한표 문서도 함께 갱신할 것

### 4.2 백엔드
- 패키지 구조는 모듈 기준으로 나눌 것
- Controller / Service / Domain(Entity) / Repository 구분 유지
- 입력 검증은 DTO 레벨과 도메인 규칙 레벨 모두 고려
- 승인/취소/마감 같은 재정 기능은 별도 application service 또는 command service로 구현
- 예외는 사용자 메시지와 내부 로그를 분리
- 감사로그는 공통 모듈로 처리

### 4.3 프런트엔드
- 반응형 UI 필수
- 목록/검색/상세/등록/수정 화면 패턴을 일관되게 유지
- 테이블 화면은 필터/페이징/정렬을 기본 제공
- 폼은 유효성 검사를 즉시 피드백
- 모바일 폭에서는 한 컬럼 레이아웃 우선

### 4.4 데이터
- MariaDB 기준으로 설계
- 테이블명은 복수형보다 프로젝트 규칙에 맞게 일관되게 사용
- 외래키와 인덱스를 명확히 정의
- 금액은 정밀도를 잃지 않는 타입 사용
- 회계 전표 라인은 합계 검증이 가능하도록 설계

## 5. Definition of Done

각 작업은 아래를 모두 만족해야 완료로 본다.

1. backlog 카드의 acceptance criteria 충족
2. 코드 빌드 성공
3. 테스트 성공
4. 린트 또는 포맷 검사 통과
5. 관련 문서 업데이트
6. 변경 파일과 영향 범위를 요약한 작업 결과 작성

## 6. 테스트 원칙

- 백엔드는 서비스 계층 테스트 우선
- 재정 규칙은 단위 테스트 필수
- 프런트는 핵심 화면 렌더링 및 폼 검증 테스트 우선
- 회귀 위험이 큰 승인/취소/마감 기능은 테스트 없이 머지 금지

## 7. 금지사항

- 문서에 없는 핵심 정책을 임의로 확정하지 말 것
- 운영 비밀값을 코드에 하드코딩하지 말 것
- 운영 DB를 직접 쓰는 가정으로 구현하지 말 것
- 전체 프로젝트를 한 번에 재구성하지 말 것
- 불필요한 기술스택 변경 제안 금지

## 8. 권장 작업 방식

좋은 예:
- `F02-single-entry-voucher.md`를 구현하고 테스트를 추가하라
- 회원 CRUD API와 화면을 만들고 acceptance criteria를 만족시켜라

좋지 않은 예:
- 교회 ERP 전체를 완성하라
- 재정 시스템을 알아서 설계해라

## 9. 결과 보고 형식

작업이 끝나면 아래 형식으로 요약한다.

```text
작업 카드:
변경 요약:
주요 파일:
테스트 결과:
남은 이슈:
문서 갱신:
```
